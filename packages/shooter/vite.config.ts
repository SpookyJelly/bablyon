import { defineConfig } from "vite";

export default defineConfig({
  assetsInclude: [
    "**/*.png",
    "**/*.jpg",
    "**/*.glb",
    "**/*.gltf",
    "**/*.babylon",
    "**/*.env",
    "**/*.mp3",
  ],
});
